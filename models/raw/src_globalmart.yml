version: 2

sources:
  - name: globalmart
    database: raw
    description: a clone of the Snowflake Database
    schema: globalmart
    tables:
      - name: customers
      - name: orders
        description: raw orders table
        columns:
          - name: orderid
            tests:
              - unique
              - not_null
      - name: product
